import styled from 'styled-components';
import { Form } from 'antd';

export const AdminFormSC = styled(Form)`
    text-align: right;`;

export const AdminFormItemSC = styled(Form.Item)`
    display: flex;
    justify-content: space-between;
    flex: 100%;`;
